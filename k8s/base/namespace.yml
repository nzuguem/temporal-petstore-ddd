apiVersion: v1
kind: Namespace
metadata:
  # Overridden by each overlay through `namespace:`, and used as the
  # replacement source for the Temporal namespace, route path and Nexus prefix.
  name: variant
