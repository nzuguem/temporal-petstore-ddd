# The variant name is spelled out here once; everything else is derived from it
# by the `variant` component applied one level up.
namespace: pinned-allatonce

resources:
  - ../../../base

components:
  - ../../../components/pinned
  - ../../../components/allatonce
